pipeline {
    agent {
		docker { image 'ajay27jsr/money_api' }
		}

    stages {
        stage('Test') {
            steps {
                sh 'docker run -p 5000:5000 money_api'
            }
        }
    }
}
